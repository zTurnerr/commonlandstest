import React from 'react';
import FormLogin from './components/FormLogin';

const Index = ({ route }) => {
    return (
        <>
            <FormLogin route={route} />
        </>
    );
};

export default Index;
